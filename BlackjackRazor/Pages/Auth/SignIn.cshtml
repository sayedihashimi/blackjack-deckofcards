@page
@model BlackjackRazor.Pages.Auth.SignInModel
@{
    ViewData["Title"] = "Sign In";
}
<div class="max-w-md mx-auto space-y-6">
  <h1 class="text-3xl font-bold">Enter Username</h1>
  <p class="text-sm opacity-75">Single-player Blackjack. Pick a username to track statistics.</p>
  @if (!string.IsNullOrEmpty(Model.Error))
  {
    <div class="bg-red-900/40 border border-red-700 text-red-200 p-3 rounded">@Model.Error</div>
  }
  <form method="post" class="space-y-4">
    <div>
      <label for="username" class="block text-sm font-medium mb-1">Username</label>
      <input id="username" name="Username" value="@Model.Username" class="w-full rounded bg-black/40 border border-white/20 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-teal-500" minlength="2" maxlength="24" pattern="[A-Za-z0-9_]+" required />
      <p class="text-xs mt-1 opacity-60">2–24 chars, letters, numbers, underscore.</p>
    </div>
    <button type="submit" class="px-4 py-2 rounded bg-teal-600 hover:bg-teal-500 font-semibold shadow">Start</button>
    @Html.AntiForgeryToken()
  </form>
</div>
